// maybe useful for later, actual counts from the real game, missing extra piece though
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const pieces = {
  LShape: 20,
  TShape: 18,
  IShape: 12,
};

export const playerColors = ["blue", "red", "green", "magenta"];
